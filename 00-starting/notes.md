## Intalling java compiler

```shell
sudo dnf install java-25-openjdk-devel
```

To compile a main class and then executing it:
```shell
javac <mainclass>.java
java <mainclass>
```

Tu compile and excecute a single file:
```shell
java file.java
```
